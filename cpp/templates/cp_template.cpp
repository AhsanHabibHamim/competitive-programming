// Competitive Programming template (C++17)
// Build with -DLOCAL hole dbg(x) kaj korbe, judge e kichu print hobe na.
#include <bits/stdc++.h>
using namespace std;

using ll = long long;
#define all(x) (x).begin(), (x).end()
#define sz(x) (int)(x).size()

#ifdef LOCAL
#define dbg(x) cerr << "[" << #x << "] = " << (x) << endl
#else
#define dbg(x)
#endif

void solve() {

}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int t = 1;
    // cin >> t;
    while (t--) solve();
    return 0;
}
